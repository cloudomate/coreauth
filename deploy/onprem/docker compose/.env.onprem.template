# ═══════════════════════════════════════════════════════════════
# CoreAuth On-Prem Configuration
# ═══════════════════════════════════════════════════════════════

# ── REQUIRED: External Database ──────────────────────────────
# Provide your PostgreSQL connection details.
# CoreAuth does NOT bundle a database — you must provide one.
POSTGRES_HOST=your-db-host
POSTGRES_PORT=5432
POSTGRES_USER=coreauth
POSTGRES_PASSWORD=your-db-password
POSTGRES_DB=coreauth
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ── REQUIRED: External Redis ─────────────────────────────────
REDIS_URL=redis://your-redis-host:6379

# ── REQUIRED: Tenant Bootstrap ───────────────────────────────
# These are used to create your initial tenant and admin user.
TENANT_NAME=My Organization
TENANT_SLUG=my-org
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=__GENERATE__
ADMIN_FULL_NAME=Admin User

# ── REQUIRED: Application ────────────────────────────────────
# OAuth application created during bootstrap.
APP_NAME=My Application
APP_SLUG=my-app
APP_TYPE=webapp
APP_CALLBACK_URLS=http://localhost:3000/callback

# ── Auto-generated (do not edit) ─────────────────────────────
JWT_SECRET=__GENERATE__

# ── Server ───────────────────────────────────────────────────
BACKEND_PORT=3000
APP_URL=http://localhost:3000
CORS_ORIGINS=*

# ── Email (optional) ─────────────────────────────────────────
EMAIL_PROVIDER=console
EMAIL_FROM=noreply@coreauth.local
EMAIL_FROM_NAME=CoreAuth
REQUIRE_EMAIL_VERIFICATION=false

# SMTP example:
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-smtp-password

# ── SMS (optional) ───────────────────────────────────────────
SMS_ENABLED=false
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=

# ── Logging ──────────────────────────────────────────────────
RUST_LOG=info

# ── Dashboard (only used with --with-dashboard) ─────────────
FRONTEND_PORT=8080
VITE_API_URL=http://localhost:3000
